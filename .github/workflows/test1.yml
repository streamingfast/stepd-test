# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the main branch
  push:
    tags: 
      - '*'
      - '!pre-*'

jobs:
  somethest:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      - name: echo blah
        run: echo blah         

      - name: Run another multi-line script
        if: ${{ startsWith(github.ref, 'refs/tags/') && github.repository == github.repository }}
        run: |
          echo bloh with some ${{ github.ref }}

#
#      - run: cp $(which date) blah
#      - name: Upload file
#        uses: actions/upload-artifact@v2
#        with:
#          name: datefile
#          path: blah
#
#  download:
#    runs-on: ubuntu-latest
#    needs: upload
#    steps:
#      - name: Retrieve built graphman
#        uses: actions/download-artifact@v2
#        with:
#          name: datefile
#          path: blahnew
#      - name: look
#        run: find .
#

#      - name: Upload timebench
#        uses: actions/upload-artifact@v2
#        with:
#          name: time
#          path: out*.time
#
